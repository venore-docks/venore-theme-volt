export { voltManifest } from "./manifest";
// Shell reaproveita a árvore de composição do Venore Slime (header em cima, sidebar+conteúdo
// abaixo) — os componentes só consomem tokens (regra 4), então a identidade "bento/dopamine" do
// Volt vem inteira de theme.css, não de código próprio de layout.
export { Shell } from "./components/Shell";
export { VOLT_COLOR_PALETTES } from "./color-palettes";
